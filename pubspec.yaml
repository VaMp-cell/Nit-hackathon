name: citypulse_mock
description: CityPulse mock/demo (runs on Flutter web without Firebase)
publish_to: 'none'
version: 0.0.1
environment:
  sdk: '>=2.18.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  provider: ^6.0.5
  uuid: ^4.3.0
  flutter_spinkit: ^5.1.0
  intl: ^0.18.0
dev_dependencies:
  flutter_test:
    sdk: flutter
